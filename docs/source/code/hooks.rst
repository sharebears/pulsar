Request Hooks
=============

``before_hook`` and ``after_hook``, respectively, run before and after each request is handled by
its controller function (if one exists).

.. automodule:: pulsar.hooks
    :members:
    :show-inheritance:
